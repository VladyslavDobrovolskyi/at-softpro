# 🟢 Тести форми «Отримати консультацію»

**Файл тестів:** `consultation-form.spec.ts`

---

## 🔍 Огляд

Тести перевіряють форму «Отримати консультацію» (поля: `Ваш ПІБ*`, `Email*`, `Телефон*`, `Ваше повідомлення*`) на сторінці https://softpro.ua/uk. Мета — перевірити, що:

- при коректних даних відправляється POST-запит на API;
- при некоректних або неповних даних запит не відправляється;
- інші небезпечні введення (XSS/SQLi) не призводять до падіння сторінки.

---

## ✅ Позитивні сценарії

**Тест:** `submits form with valid data and sends POST` ✅

- Підготовка: відкрити головну і натиснути `Отримати консультацію` (відкриває форму)
- Кроки: заповнити `Ваш ПІБ` / `Email` / `Телефон` / `Ваше повідомлення` валідними значеннями → натиснути `Надіслати`.
- Очікування: клієнт робить POST-запит на `/api/...`, у тілі присутні поля з відповідними значеннями.

---

## ❌ Негативні сценарії

**Тест:** `required fields block submission` ❌

- Перевірки:
    - Всі поля пусті → не надсилається запит.
    - Відсутній текст повідомлення → не надсилається запит або браузер показує валідатор (перевірка `checkValidity()`).

---

## ✉️ Email validation

**Тест:** `email validation adheres to email checklist`

- Тестуємо ряд типових невірних email (`plainaddress.com`, `test@@example.com`, `test@`, `@example.com`, `test..user@example.com`)
- Очікування: форма не надсилає запит при помічених помилках; якщо браузер не блокує — сервер не отримує запит в короткий таймаут.

---

## 🔒 XSS/SQLi у полі повідомлення

**Тест:** `XSS/SQLi in message should not break page`

- Вставляємо payload (`<script>alert(1)</script>` або `OR 1=1`) у повідомлення і відправляємо.
- Очікування: сторінка не показує `500` і не падає; якщо запит на сервер присутній — в тілі запиту бачимо введені дані (сервер має виконати санітизацію).

---

## 📝 Примітки

- Рекомендується в CI мокати `POST /api/*` щоб робити тести стабільними і керувати кодами відповідей (200/400/500).
- Для генерації даних і property-based тестування використовуються бібліотеки:
    - **@faker-js/faker** — для реалістичних імен, телефонів і текстів;
    - **fast-check** — для генерації випадкових email у property-based тесті (зразки).
- Запуск локально:

```bash
npx playwright test consultation-form.spec.ts
```

---

Автор: Vladyslav Dobrovolksyi (Powered by AI)
Дата: 2026-02-02
